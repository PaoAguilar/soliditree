import React from 'react';
import { Navbar } from '../src/components/Navbar/Navbar';

const withdraw = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default withdraw;